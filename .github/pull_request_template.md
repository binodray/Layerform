## Summary

Describe the problem and the approach taken.

## Verification

- [ ] `dotnet build src/Compositor.App/Compositor.App.csproj -c Debug -p:Platform=x64`
- [ ] `dotnet test tests/Compositor.Core.Tests/Compositor.Core.Tests.csproj -c Debug`
- [ ] I tested the affected workflow in the Windows application.

## UI changes

Add before/after screenshots when the change is visible. Write “Not applicable” otherwise.

## Compatibility and attribution

- [ ] Project-format compatibility is preserved or the format change is documented and versioned.
- [ ] New dependencies, assets and translated upstream code have appropriate notices.
- [ ] User-facing behavior changes are documented in `CHANGELOG.md`.

